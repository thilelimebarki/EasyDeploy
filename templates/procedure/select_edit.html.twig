{% extends 'base.html.twig' %}

{% block stylesheets %}
	<style>

		.app-container {
			background-color: rgba(255, 255, 255, 0.95);
			max-width: 1000px;
			margin: 60px auto;
			padding: 30px;
			border-radius: 20px;
		}

		h1 {
			text-align: center;
			font-size: 40px;
			font-style: italic;
			margin-bottom: 10px;
		}

		p {
			font-size: 1.1rem;
			text-align: center;
			margin-bottom: 30px;
		}

		table {
			width: 100%;
			border-collapse: collapse;
		}

		th,
		td {
			font-size: 1.1rem;
			padding: 15px;
			text-align: center;
		}

		th {
			border-bottom: 2px solid #ccc;
		}

		td {
			border-bottom: 1px solid #eee;
		}

		.btn-cancel {
			background: #9E9E9E;
			color: black;
			display: inline-block;
			padding: 10px 18px;
			border-radius: 6px;
			font-size: 1.1rem;
			text-decoration: none;
			transition: background 0.3s, transform 0.2s;
		}

		.btn-edit {
			background-color: #9E9E9E;
			padding: 8px 12px;
			border: none;
			border-radius: 5px;
			text-decoration: none;
			color: white;
			cursor: pointer;
			margin: 5px;
			display: inline-block;
		}

		.btn:hover {
			background: #5a6268;
			transform: scale(1.05);
		}
	</style>
{% endblock %}

{% block body %}
	<div class="app-container">
		<h1>
			Modifier une procédure
		</h1>
		<p>
			Sélectionnez une procédure à modifier.
		</p>

		<table>
			<thead>
				<tr>
					<th>
						Procédure
					</th>
					<th>
						Descriptions
					</th>
					<th>
						Action
					</th>
				</tr>
			</thead>
			<tbody>
				{% for procedure in procedures %}
					<tr>
						<td>
							{{ procedure.nom }}
						</td>
						<td>
							{{ procedure.description }}
						</td>
						<td>
							<a href="{{ path('app_procedure_edit', { id: procedure.id }) }}" class="btn btn-edit">
								Modifier
							</a>
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		<a href="{{ path('app_procedure_index') }}" class="btn btn-cancel">
			↩ Retour
		</a>
		<a href="{{ path('dashboard') }}" class="btn btn-cancel">
			Accueil
		</a>
	</div>
{% endblock %}
